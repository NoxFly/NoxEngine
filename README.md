# C++ 3D Engine

## Technologies

SDL2, SDL2_image, SDL2_ttf, SDL2_mixer, glew, opengl.

Willing to make it work with Vulkan and DirectX in further developments.

Working with CMake, on Windows and Linux.

## Build

```shell
cmake -B build --preset=debug-local
cmake --build build --config Debug
```